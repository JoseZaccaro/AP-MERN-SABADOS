import React from 'react'
// import { useSelector } from 'react-redux';

const Peliculas = () => {
  // const valor = useSelector((store) => store.series)
  // console.log(valor);
  return (
    <div>Peliculas</div>
  )
}

export default Peliculas